# Backend (API + SQLite + WebSocket) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ein Fastify-Server, der Spiele in SQLite anlegt, per unratbarem Slug ausliefert,
Würfe/Undo/Beitritt/Verlängern verarbeitet, alle Änderungen live über WebSocket
broadcastet, abgelaufene Spiele löscht und gegen Überlastung (Rate-Limiting) schützt.

**Architecture:** Das Event-Log (`throw_events`) ist die Quelle der Wahrheit; jeder
Lese-Endpunkt berechnet den Zustand frisch über die Engine (Plan 1/2). Mutationen sind
dünne Handler: validieren → in SQLite schreiben → Zustand neu berechnen → an alle Clients
im Spiel-Raum broadcasten.

**Tech Stack:** Node ≥ 20, TypeScript strict, ESM, Fastify, `@fastify/websocket`,
`@fastify/rate-limit`, `better-sqlite3` (synchron, dateibasiert), `nanoid` (Slugs/IDs),
Vitest (Integrationstests via `app.inject`).

**Voraussetzung:** Plan 1 + 2 umgesetzt (Engine grün).

## Global Constraints

- TS strict, ESM, Node 20 (wie Plan 1).
- SQLite-Datei via ENV `DB_PATH` (Default `./data/dart.sqlite`); Tests nutzen `:memory:`.
- Slugs/Player-IDs: `nanoid`, URL-sicheres Alphabet, Slug-Länge 10.
- Ein Spiel läuft **24h ab Erstellung**; `expires_at = created_at + 86_400_000` (ms).
- Verlängern-Optionen: `+1 Tag | +1 Woche | +1 Monat` (Server akzeptiert `'1d'|'1w'|'1M'`).
- Rate-Limits (Startwerte): **Spiel-Erstellung ≤ 20 / Stunde / IP**; **globaler Deckel
  10_000 aktive Spiele**; **WS-Verbindungen ≤ 50 / IP**.
- Commits: nur Meilensteine, Autor nur User.

---

## File Structure

```
server/
  package.json            # + fastify, @fastify/websocket, @fastify/rate-limit, better-sqlite3, nanoid
  src/
    db.ts                 # SQLite öffnen + Schema
    repo.ts               # Datenzugriff (Games, Players, Events)
    ids.ts                # slug()/playerId()
    engine/index.ts       # computeGameState-Dispatcher (x01/cricket/atc)
    api/
      games.ts            # POST /api/games, GET /api/games/:slug
      play.ts             # POST players/throws/undo/extend
    realtime.ts           # WebSocket-Räume + broadcast
    cleanup.ts            # Löschen abgelaufener Spiele
    app.ts                # Fastify-App zusammenbauen (Plugins, Routen)
    server.ts             # Einstiegspunkt (listen)
    __tests__/
      repo.test.ts
      engine-index.test.ts
      api-games.test.ts
      api-play.test.ts
      cleanup.test.ts
```

---

### Task 1: SQLite-Schema + Games-Repo (create/get)

**Files:**
- Modify: `server/package.json`
- Create: `server/src/db.ts`, `server/src/ids.ts`, `server/src/repo.ts`
- Test: `server/src/__tests__/repo.test.ts`

**Interfaces:**
- `openDb(path: string): Database` — better-sqlite3 Instanz mit erzeugtem Schema.
- `ids.slug(): string`, `ids.playerId(): string`.
- ```ts
  interface GameRow { id: number; slug: string; gameType: GameType; options: unknown; status: 'lobby'|'running'|'finished'; createdAt: number; expiresAt: number; }
  function createGame(db, input: { gameType: GameType; options: unknown; now: number }): GameRow
  function getGameBySlug(db, slug: string): GameRow | null
  ```

- [ ] **Step 1: Dependencies ergänzen**

`server/package.json` — `dependencies` hinzufügen und `devDependencies` erweitern:
```json
  "dependencies": {
    "@fastify/rate-limit": "^9.1.0",
    "@fastify/websocket": "^10.0.0",
    "better-sqlite3": "^11.0.0",
    "fastify": "^4.28.0",
    "nanoid": "^5.0.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
```
Run: `cd server && npm install`
Expected: Installation ohne Fehler.

- [ ] **Step 2: Failing test schreiben**

`server/src/__tests__/repo.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { openDb } from '../db.js';
import { createGame, getGameBySlug } from '../repo.js';

describe('Games-Repo', () => {
  it('legt ein Spiel an und liest es per Slug', () => {
    const db = openDb(':memory:');
    const g = createGame(db, { gameType: 'x01', options: { start: 501, in: 'straight', out: 'double' }, now: 1000 });
    expect(g.slug).toHaveLength(10);
    expect(g.expiresAt).toBe(1000 + 86_400_000);
    const loaded = getGameBySlug(db, g.slug);
    expect(loaded?.gameType).toBe('x01');
    expect(loaded?.options).toEqual({ start: 501, in: 'straight', out: 'double' });
  });
  it('unbekannter Slug -> null', () => {
    const db = openDb(':memory:');
    expect(getGameBySlug(db, 'nope')).toBeNull();
  });
});
```

- [ ] **Step 3: Test laufen lassen — muss fehlschlagen**

Run: `cd server && npx vitest run src/__tests__/repo.test.ts`
Expected: FAIL — Module fehlen.

- [ ] **Step 4: `db.ts` implementieren**

`server/src/db.ts`:
```ts
import Database from 'better-sqlite3';

export type DB = Database.Database;

export function openDb(path: string): DB {
  const db = new Database(path);
  db.pragma('journal_mode = WAL');
  db.pragma('foreign_keys = ON');
  db.exec(`
    CREATE TABLE IF NOT EXISTS games (
      id INTEGER PRIMARY KEY AUTOINCREMENT,
      slug TEXT NOT NULL UNIQUE,
      game_type TEXT NOT NULL,
      options TEXT NOT NULL,
      status TEXT NOT NULL DEFAULT 'lobby',
      created_at INTEGER NOT NULL,
      expires_at INTEGER NOT NULL
    );
    CREATE TABLE IF NOT EXISTS players (
      id TEXT PRIMARY KEY,
      game_id INTEGER NOT NULL REFERENCES games(id) ON DELETE CASCADE,
      name TEXT NOT NULL,
      order_idx INTEGER NOT NULL,
      joined_at_round INTEGER NOT NULL DEFAULT 0,
      catch_up TEXT NOT NULL DEFAULT 'handicap'
    );
    CREATE TABLE IF NOT EXISTS throw_events (
      id INTEGER PRIMARY KEY AUTOINCREMENT,
      game_id INTEGER NOT NULL REFERENCES games(id) ON DELETE CASCADE,
      seq INTEGER NOT NULL,
      player_id TEXT NOT NULL,
      segment INTEGER NOT NULL,
      multiplier INTEGER NOT NULL
    );
    CREATE INDEX IF NOT EXISTS idx_events_game ON throw_events(game_id, seq);
    CREATE INDEX IF NOT EXISTS idx_players_game ON players(game_id, order_idx);
    CREATE INDEX IF NOT EXISTS idx_games_expires ON games(expires_at);
  `);
  return db;
}
```

- [ ] **Step 5: `ids.ts` implementieren**

`server/src/ids.ts`:
```ts
import { customAlphabet } from 'nanoid';

const alphabet = '0123456789abcdefghijklmnopqrstuvwxyz';
const makeSlug = customAlphabet(alphabet, 10);
const makeId = customAlphabet(alphabet, 16);

export const slug = (): string => makeSlug();
export const playerId = (): string => makeId();
```

- [ ] **Step 6: `repo.ts` (Games) implementieren**

`server/src/repo.ts`:
```ts
import type { DB } from './db.js';
import type { GameType } from './engine/index.js';
import * as ids from './ids.js';

export interface GameRow {
  id: number;
  slug: string;
  gameType: GameType;
  options: unknown;
  status: 'lobby' | 'running' | 'finished';
  createdAt: number;
  expiresAt: number;
}

const DAY_MS = 86_400_000;

interface GameDbRow {
  id: number; slug: string; game_type: string; options: string;
  status: string; created_at: number; expires_at: number;
}

function mapGame(r: GameDbRow): GameRow {
  return {
    id: r.id, slug: r.slug, gameType: r.game_type as GameType,
    options: JSON.parse(r.options), status: r.status as GameRow['status'],
    createdAt: r.created_at, expiresAt: r.expires_at,
  };
}

export function createGame(db: DB, input: { gameType: GameType; options: unknown; now: number }): GameRow {
  const s = ids.slug();
  const expiresAt = input.now + DAY_MS;
  const info = db.prepare(
    `INSERT INTO games (slug, game_type, options, status, created_at, expires_at)
     VALUES (?, ?, ?, 'lobby', ?, ?)`,
  ).run(s, input.gameType, JSON.stringify(input.options), input.now, expiresAt);
  const row = db.prepare('SELECT * FROM games WHERE id = ?').get(info.lastInsertRowid) as GameDbRow;
  return mapGame(row);
}

export function getGameBySlug(db: DB, slug: string): GameRow | null {
  const row = db.prepare('SELECT * FROM games WHERE slug = ?').get(slug) as GameDbRow | undefined;
  return row ? mapGame(row) : null;
}
```

*(Hinweis: `GameType` wird in Task 3 aus `engine/index.ts` re-exportiert; bis dahin
scheitert der Typecheck an diesem Import — Task 3 wird VOR dem finalen Typecheck
umgesetzt. Für Task 1 genügt `npx vitest run` (Runtime), Typecheck folgt in Task 3.)*

- [ ] **Step 7: Test laufen lassen — muss grün sein**

Run: `cd server && npx vitest run src/__tests__/repo.test.ts`
Expected: PASS.

- [ ] **Step 8: Commit**

```bash
git add server/
git commit -m "Backend: SQLite-Schema + Games-Repo (create/get)"
```

---

### Task 2: Repo — Players & Events (append/list/undo)

**Files:**
- Modify: `server/src/repo.ts`
- Test: `server/src/__tests__/repo.test.ts`

**Interfaces:**
- ```ts
  interface PlayerRow { id: string; name: string; order: number; joinedAtRound: number; catchUp: 'catchUp'|'handicap'; }
  function addPlayer(db, gameId: number, input: { name: string; joinedAtRound?: number; catchUp?: 'catchUp'|'handicap' }): PlayerRow
  function listPlayers(db, gameId: number): PlayerRow[]
  interface EventRow { seq: number; playerId: string; segment: number; multiplier: number; }
  function appendThrow(db, gameId: number, dart: { playerId: string; segment: number; multiplier: number }): EventRow
  function listThrows(db, gameId: number): EventRow[]
  function undoLastThrow(db, gameId: number): EventRow | null
  function setStatus(db, gameId: number, status: 'lobby'|'running'|'finished'): void
  ```

- [ ] **Step 1: Failing tests ergänzen**

In `repo.test.ts` anfügen:
```ts
import { addPlayer, listPlayers, appendThrow, listThrows, undoLastThrow } from '../repo.js';

describe('Players & Events', () => {
  it('fügt Spieler in Reihenfolge hinzu', () => {
    const db = openDb(':memory:');
    const g = createGame(db, { gameType: 'x01', options: {}, now: 0 });
    const a = addPlayer(db, g.id, { name: 'A' });
    const b = addPlayer(db, g.id, { name: 'B' });
    expect(a.order).toBe(0);
    expect(b.order).toBe(1);
    expect(listPlayers(db, g.id).map(p => p.name)).toEqual(['A', 'B']);
  });
  it('append vergibt fortlaufende seq, undo entfernt den letzten', () => {
    const db = openDb(':memory:');
    const g = createGame(db, { gameType: 'x01', options: {}, now: 0 });
    const a = addPlayer(db, g.id, { name: 'A' });
    const e1 = appendThrow(db, g.id, { playerId: a.id, segment: 20, multiplier: 3 });
    const e2 = appendThrow(db, g.id, { playerId: a.id, segment: 20, multiplier: 1 });
    expect(e1.seq).toBe(1);
    expect(e2.seq).toBe(2);
    expect(listThrows(db, g.id)).toHaveLength(2);
    const removed = undoLastThrow(db, g.id);
    expect(removed?.seq).toBe(2);
    expect(listThrows(db, g.id)).toHaveLength(1);
  });
  it('undo bei leerem Log -> null', () => {
    const db = openDb(':memory:');
    const g = createGame(db, { gameType: 'x01', options: {}, now: 0 });
    expect(undoLastThrow(db, g.id)).toBeNull();
  });
});
```

- [ ] **Step 2: Test laufen lassen — muss fehlschlagen**

Run: `cd server && npx vitest run src/__tests__/repo.test.ts`
Expected: FAIL — Funktionen fehlen.

- [ ] **Step 3: Implementierung ergänzen**

Am Ende von `server/src/repo.ts` anfügen:
```ts
export interface PlayerRow {
  id: string; name: string; order: number; joinedAtRound: number; catchUp: 'catchUp' | 'handicap';
}
interface PlayerDbRow { id: string; name: string; order_idx: number; joined_at_round: number; catch_up: string; }
function mapPlayer(r: PlayerDbRow): PlayerRow {
  return { id: r.id, name: r.name, order: r.order_idx, joinedAtRound: r.joined_at_round, catchUp: r.catch_up as PlayerRow['catchUp'] };
}

export function addPlayer(
  db: DB,
  gameId: number,
  input: { name: string; joinedAtRound?: number; catchUp?: 'catchUp' | 'handicap' },
): PlayerRow {
  const next = db.prepare('SELECT COALESCE(MAX(order_idx) + 1, 0) AS n FROM players WHERE game_id = ?').get(gameId) as { n: number };
  const id = ids.playerId();
  db.prepare(
    `INSERT INTO players (id, game_id, name, order_idx, joined_at_round, catch_up)
     VALUES (?, ?, ?, ?, ?, ?)`,
  ).run(id, gameId, input.name, next.n, input.joinedAtRound ?? 0, input.catchUp ?? 'handicap');
  return { id, name: input.name, order: next.n, joinedAtRound: input.joinedAtRound ?? 0, catchUp: input.catchUp ?? 'handicap' };
}

export function listPlayers(db: DB, gameId: number): PlayerRow[] {
  const rows = db.prepare('SELECT * FROM players WHERE game_id = ? ORDER BY order_idx').all(gameId) as PlayerDbRow[];
  return rows.map(mapPlayer);
}

export interface EventRow { seq: number; playerId: string; segment: number; multiplier: number; }
interface EventDbRow { seq: number; player_id: string; segment: number; multiplier: number; }

export function appendThrow(
  db: DB,
  gameId: number,
  dart: { playerId: string; segment: number; multiplier: number },
): EventRow {
  const next = db.prepare('SELECT COALESCE(MAX(seq) + 1, 1) AS n FROM throw_events WHERE game_id = ?').get(gameId) as { n: number };
  db.prepare(
    `INSERT INTO throw_events (game_id, seq, player_id, segment, multiplier) VALUES (?, ?, ?, ?, ?)`,
  ).run(gameId, next.n, dart.playerId, dart.segment, dart.multiplier);
  return { seq: next.n, playerId: dart.playerId, segment: dart.segment, multiplier: dart.multiplier };
}

export function listThrows(db: DB, gameId: number): EventRow[] {
  const rows = db.prepare('SELECT seq, player_id, segment, multiplier FROM throw_events WHERE game_id = ? ORDER BY seq').all(gameId) as EventDbRow[];
  return rows.map((r) => ({ seq: r.seq, playerId: r.player_id, segment: r.segment, multiplier: r.multiplier }));
}

export function undoLastThrow(db: DB, gameId: number): EventRow | null {
  const row = db.prepare('SELECT seq, player_id, segment, multiplier FROM throw_events WHERE game_id = ? ORDER BY seq DESC LIMIT 1').get(gameId) as EventDbRow | undefined;
  if (!row) return null;
  db.prepare('DELETE FROM throw_events WHERE game_id = ? AND seq = ?').run(gameId, row.seq);
  return { seq: row.seq, playerId: row.player_id, segment: row.segment, multiplier: row.multiplier };
}

export function setStatus(db: DB, gameId: number, status: 'lobby' | 'running' | 'finished'): void {
  db.prepare('UPDATE games SET status = ? WHERE id = ?').run(status, gameId);
}
```

- [ ] **Step 4: Test laufen lassen**

Run: `cd server && npx vitest run src/__tests__/repo.test.ts`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add server/src/repo.ts server/src/__tests__/repo.test.ts
git commit -m "Backend: Repo für Players & Events (append/list/undo)"
```

---

### Task 3: Engine-Dispatcher `computeGameState`

Routet je nach Spielart an die passende Engine und liefert ein einheitliches Ergebnis.

**Files:**
- Create: `server/src/engine/index.ts`
- Test: `server/src/__tests__/engine-index.test.ts`

**Interfaces:**
- Re-Export `GameType` (aus `engine/types.js`).
- ```ts
  type GameStateResult =
    | { gameType: 'x01'; state: X01State }
    | { gameType: 'cricket'; state: CricketState }
    | { gameType: 'aroundTheClock'; state: AtcState };
  function computeGameState(gameType: GameType, options: unknown, players: PlayerInput[], darts: PlayerDart[]): GameStateResult
  function currentPlayerId(result: GameStateResult): string | null
  function isFinished(result: GameStateResult): { finished: boolean; winnerId: string | null }
  ```

- [ ] **Step 1: Failing test schreiben**

`server/src/__tests__/engine-index.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { computeGameState, currentPlayerId, isFinished } from '../engine/index.js';
import type { PlayerInput, PlayerDart } from '../engine/types.js';

const PLAYERS: PlayerInput[] = [{ id: 'a', name: 'A', order: 0 }, { id: 'b', name: 'B', order: 1 }];

describe('computeGameState', () => {
  it('routet x01', () => {
    const r = computeGameState('x01', { start: 501, in: 'straight', out: 'straight' }, PLAYERS, []);
    expect(r.gameType).toBe('x01');
    expect(currentPlayerId(r)).toBe('a');
    expect(isFinished(r).finished).toBe(false);
  });
  it('routet cricket', () => {
    const r = computeGameState('cricket', { mode: 'standard', bull: true }, PLAYERS, []);
    expect(r.gameType).toBe('cricket');
  });
  it('routet aroundTheClock', () => {
    const darts: PlayerDart[] = [];
    const r = computeGameState('aroundTheClock', { advanceByMultiplier: true }, PLAYERS, darts);
    expect(r.gameType).toBe('aroundTheClock');
    expect(currentPlayerId(r)).toBe('a');
  });
});
```

- [ ] **Step 2: Test laufen lassen — muss fehlschlagen**

Run: `cd server && npx vitest run src/__tests__/engine-index.test.ts`
Expected: FAIL — `engine/index.js` fehlt.

- [ ] **Step 3: Dispatcher implementieren**

`server/src/engine/index.ts`:
```ts
import type { GameType, PlayerDart, PlayerInput, X01Options, CricketOptions, AtcOptions } from './types.js';
import { computeX01State, type X01State } from './x01.js';
import { computeCricketState, type CricketState } from './cricket.js';
import { computeAtcState, type AtcState } from './atc.js';

export type { GameType } from './types.js';

export type GameStateResult =
  | { gameType: 'x01'; state: X01State }
  | { gameType: 'cricket'; state: CricketState }
  | { gameType: 'aroundTheClock'; state: AtcState };

export function computeGameState(
  gameType: GameType,
  options: unknown,
  players: PlayerInput[],
  darts: PlayerDart[],
): GameStateResult {
  switch (gameType) {
    case 'x01':
      return { gameType, state: computeX01State(options as X01Options, players, darts) };
    case 'cricket':
      return { gameType, state: computeCricketState(options as CricketOptions, players, darts) };
    case 'aroundTheClock':
      return { gameType, state: computeAtcState(options as AtcOptions, players, darts) };
  }
}

export function currentPlayerId(result: GameStateResult): string | null {
  return result.state.currentPlayerId;
}

export function isFinished(result: GameStateResult): { finished: boolean; winnerId: string | null } {
  return { finished: result.state.finished, winnerId: result.state.winnerId };
}
```

- [ ] **Step 4: Tests + Typecheck (jetzt vollständig)**

Run: `cd server && npx vitest run && npx tsc --noEmit`
Expected: PASS (alle Tests inkl. repo), Typecheck sauber (der `GameType`-Import in
`repo.ts` löst sich jetzt auf).

- [ ] **Step 5: Commit**

```bash
git add server/src/engine/index.ts server/src/__tests__/engine-index.test.ts
git commit -m "Backend: Engine-Dispatcher computeGameState"
```

---

### Task 4: Fastify-App + Spiel erstellen/lesen

**Files:**
- Create: `server/src/app.ts`, `server/src/api/games.ts`
- Test: `server/src/__tests__/api-games.test.ts`

**Interfaces:**
- `buildApp(db: DB, now?: () => number): FastifyInstance` — App ohne `listen`, testbar via `inject`.
- `POST /api/games` Body `{ gameType, options, players: string[] }` → `201 { slug }`.
- `GET /api/games/:slug` → `200 { slug, gameType, options, status, createdAt, expiresAt, players, state, history }` oder `404`.
- `buildGameView(db, game): GameView` (in `api/games.ts`, auch von Play-Routen genutzt).

- [ ] **Step 1: Failing test schreiben**

`server/src/__tests__/api-games.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { openDb } from '../db.js';
import { buildApp } from '../app.js';

describe('API: Spiele', () => {
  it('erstellt ein Spiel und liefert es per Slug', async () => {
    const app = buildApp(openDb(':memory:'), () => 1000);
    const create = await app.inject({
      method: 'POST', url: '/api/games',
      payload: { gameType: 'x01', options: { start: 501, in: 'straight', out: 'double' }, players: ['Mia', 'Ben'] },
    });
    expect(create.statusCode).toBe(201);
    const { slug } = create.json();
    expect(slug).toHaveLength(10);

    const get = await app.inject({ method: 'GET', url: `/api/games/${slug}` });
    expect(get.statusCode).toBe(200);
    const body = get.json();
    expect(body.gameType).toBe('x01');
    expect(body.players.map((p: { name: string }) => p.name)).toEqual(['Mia', 'Ben']);
    expect(body.state.currentPlayerId).toBe(body.players[0].id);
    expect(body.expiresAt).toBe(1000 + 86_400_000);
    await app.close();
  });
  it('unbekannter Slug -> 404', async () => {
    const app = buildApp(openDb(':memory:'));
    const res = await app.inject({ method: 'GET', url: '/api/games/unknown' });
    expect(res.statusCode).toBe(404);
    await app.close();
  });
});
```

- [ ] **Step 2: Test laufen lassen — muss fehlschlagen**

Run: `cd server && npx vitest run src/__tests__/api-games.test.ts`
Expected: FAIL — `app.js` fehlt.

- [ ] **Step 3: `api/games.ts` implementieren**

`server/src/api/games.ts`:
```ts
import type { FastifyInstance } from 'fastify';
import type { DB } from '../db.js';
import { createGame, getGameBySlug, addPlayer, listPlayers, listThrows, type GameRow } from '../repo.js';
import { computeGameState } from '../engine/index.js';
import type { PlayerInput, PlayerDart } from '../engine/types.js';

export interface GameView {
  slug: string; gameType: string; options: unknown; status: string;
  createdAt: number; expiresAt: number;
  players: { id: string; name: string; order: number; joinedAtRound: number; catchUp: string }[];
  state: unknown;
  history: { seq: number; playerId: string; segment: number; multiplier: number }[];
}

export function buildGameView(db: DB, game: GameRow): GameView {
  const players = listPlayers(db, game.id);
  const history = listThrows(db, game.id);
  const engineIn: PlayerInput[] = players.map((p) => ({ id: p.id, name: p.name, order: p.order }));
  const darts: PlayerDart[] = history.map((e) => ({ playerId: e.playerId, segment: e.segment, multiplier: e.multiplier }));
  const result = computeGameState(game.gameType, game.options, engineIn, darts);
  return {
    slug: game.slug, gameType: game.gameType, options: game.options, status: game.status,
    createdAt: game.createdAt, expiresAt: game.expiresAt,
    players, state: result.state, history,
  };
}

export function registerGameRoutes(app: FastifyInstance, db: DB, now: () => number): void {
  app.post('/api/games', async (req, reply) => {
    const body = req.body as { gameType: 'x01' | 'cricket' | 'aroundTheClock'; options: unknown; players?: string[] };
    if (!body?.gameType) return reply.code(400).send({ error: 'gameType fehlt' });
    const game = createGame(db, { gameType: body.gameType, options: body.options ?? {}, now: now() });
    for (const name of body.players ?? []) {
      if (name?.trim()) addPlayer(db, game.id, { name: name.trim().slice(0, 14) });
    }
    return reply.code(201).send({ slug: game.slug });
  });

  app.get('/api/games/:slug', async (req, reply) => {
    const { slug } = req.params as { slug: string };
    const game = getGameBySlug(db, slug);
    if (!game) return reply.code(404).send({ error: 'nicht gefunden' });
    return reply.send(buildGameView(db, game));
  });
}
```

- [ ] **Step 4: `app.ts` implementieren**

`server/src/app.ts`:
```ts
import Fastify, { type FastifyInstance } from 'fastify';
import type { DB } from './db.js';
import { registerGameRoutes } from './api/games.js';

export function buildApp(db: DB, now: () => number = () => Date.now()): FastifyInstance {
  const app = Fastify({ logger: false });
  registerGameRoutes(app, db, now);
  return app;
}
```

- [ ] **Step 5: Tests + Typecheck**

Run: `cd server && npx vitest run && npx tsc --noEmit`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add server/src/app.ts server/src/api/ server/src/__tests__/api-games.test.ts
git commit -m "Backend: Fastify-App + Spiel erstellen/lesen"
```

---

### Task 5: Spielen — Beitreten, Werfen, Undo

Der Server bestimmt beim Werfen automatisch den aktuellen Spieler (jede*r darf für die
dran-seiende Person eintragen). Erstes Werfen setzt Status `running`; Sieg setzt `finished`.

**Files:**
- Create: `server/src/api/play.ts`
- Modify: `server/src/app.ts`
- Test: `server/src/__tests__/api-play.test.ts`

**Interfaces:**
- `POST /api/games/:slug/players` Body `{ name, catchUp? }` → `201 { player }`.
- `POST /api/games/:slug/throws` Body `{ segment, multiplier }` → `200 GameView` (Dart geht an aktuellen Spieler).
- `POST /api/games/:slug/undo` → `200 GameView`.
- `registerPlayRoutes(app, db, now, onChange)` — `onChange(slug)` Hook (Broadcast in Task 7).

- [ ] **Step 1: Failing tests schreiben**

`server/src/__tests__/api-play.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { openDb } from '../db.js';
import { buildApp } from '../app.js';

async function newGame(app: ReturnType<typeof buildApp>) {
  const res = await app.inject({
    method: 'POST', url: '/api/games',
    payload: { gameType: 'x01', options: { start: 501, in: 'straight', out: 'straight' }, players: ['Mia', 'Ben'] },
  });
  return res.json().slug as string;
}

describe('API: Spielen', () => {
  it('Wurf geht an den aktuellen Spieler und zieht Punkte ab', async () => {
    const app = buildApp(openDb(':memory:'));
    const slug = await newGame(app);
    const res = await app.inject({ method: 'POST', url: `/api/games/${slug}/throws`, payload: { segment: 20, multiplier: 3 } });
    expect(res.statusCode).toBe(200);
    const view = res.json();
    const mia = view.players[0];
    expect(view.state.players.find((p: { playerId: string }) => p.playerId === mia.id).remaining).toBe(501 - 60);
    expect(view.status).toBe('running');
    await app.close();
  });

  it('Undo nimmt den letzten Wurf zurück', async () => {
    const app = buildApp(openDb(':memory:'));
    const slug = await newGame(app);
    await app.inject({ method: 'POST', url: `/api/games/${slug}/throws`, payload: { segment: 20, multiplier: 3 } });
    const res = await app.inject({ method: 'POST', url: `/api/games/${slug}/undo` });
    const view = res.json();
    expect(view.state.players[0].remaining).toBe(501);
    await app.close();
  });

  it('Beitreten während running vermerkt Runde + catchUp', async () => {
    const app = buildApp(openDb(':memory:'));
    const slug = await newGame(app);
    await app.inject({ method: 'POST', url: `/api/games/${slug}/throws`, payload: { segment: 1, multiplier: 1 } });
    const res = await app.inject({ method: 'POST', url: `/api/games/${slug}/players`, payload: { name: 'Lena', catchUp: 'catchUp' } });
    expect(res.statusCode).toBe(201);
    expect(res.json().player.catchUp).toBe('catchUp');
    await app.close();
  });

  it('Werfen im beendeten Spiel -> 409', async () => {
    const app = buildApp(openDb(':memory:'));
    const create = await app.inject({
      method: 'POST', url: '/api/games',
      payload: { gameType: 'x01', options: { start: 3, in: 'straight', out: 'straight' }, players: ['Mia'] },
    });
    const slug = create.json().slug;
    await app.inject({ method: 'POST', url: `/api/games/${slug}/throws`, payload: { segment: 3, multiplier: 1 } }); // gewinnt
    const res = await app.inject({ method: 'POST', url: `/api/games/${slug}/throws`, payload: { segment: 1, multiplier: 1 } });
    expect(res.statusCode).toBe(409);
    await app.close();
  });
});
```

- [ ] **Step 2: Test laufen lassen — muss fehlschlagen**

Run: `cd server && npx vitest run src/__tests__/api-play.test.ts`
Expected: FAIL — Play-Routen fehlen.

- [ ] **Step 3: `api/play.ts` implementieren**

`server/src/api/play.ts`:
```ts
import type { FastifyInstance } from 'fastify';
import type { DB } from '../db.js';
import {
  getGameBySlug, addPlayer, listPlayers, listThrows, appendThrow, undoLastThrow, setStatus,
} from '../repo.js';
import { computeGameState } from '../engine/index.js';
import type { PlayerInput, PlayerDart } from '../engine/types.js';
import { buildGameView } from './games.js';

function computeCurrent(db: DB, gameId: number, gameType: 'x01' | 'cricket' | 'aroundTheClock', options: unknown) {
  const players = listPlayers(db, gameId);
  const engineIn: PlayerInput[] = players.map((p) => ({ id: p.id, name: p.name, order: p.order }));
  const darts: PlayerDart[] = listThrows(db, gameId).map((e) => ({ playerId: e.playerId, segment: e.segment, multiplier: e.multiplier }));
  return computeGameState(gameType, options, engineIn, darts);
}

export function registerPlayRoutes(app: FastifyInstance, db: DB, onChange: (slug: string) => void): void {
  app.post('/api/games/:slug/players', async (req, reply) => {
    const { slug } = req.params as { slug: string };
    const game = getGameBySlug(db, slug);
    if (!game) return reply.code(404).send({ error: 'nicht gefunden' });
    const body = req.body as { name?: string; catchUp?: 'catchUp' | 'handicap' };
    if (!body?.name?.trim()) return reply.code(400).send({ error: 'name fehlt' });
    const result = computeCurrent(db, game.id, game.gameType, game.options);
    const joinedAtRound = game.status === 'running' ? result.state.round : 0;
    const player = addPlayer(db, game.id, { name: body.name.trim().slice(0, 14), joinedAtRound, catchUp: body.catchUp ?? 'handicap' });
    onChange(slug);
    return reply.code(201).send({ player });
  });

  app.post('/api/games/:slug/throws', async (req, reply) => {
    const { slug } = req.params as { slug: string };
    const game = getGameBySlug(db, slug);
    if (!game) return reply.code(404).send({ error: 'nicht gefunden' });
    const before = computeCurrent(db, game.id, game.gameType, game.options);
    if (before.state.finished) return reply.code(409).send({ error: 'Spiel beendet' });
    const current = before.state.currentPlayerId;
    if (!current) return reply.code(409).send({ error: 'kein aktiver Spieler' });

    const body = req.body as { segment?: number; multiplier?: number };
    const segment = Number(body?.segment);
    const multiplier = Number(body?.multiplier);
    if (!Number.isInteger(segment) || !Number.isInteger(multiplier) || segment < 0 || segment > 25 || multiplier < 0 || multiplier > 3) {
      return reply.code(400).send({ error: 'ungültiger Dart' });
    }
    if (game.status === 'lobby') setStatus(db, game.id, 'running');
    appendThrow(db, game.id, { playerId: current, segment, multiplier });

    const after = computeCurrent(db, game.id, game.gameType, game.options);
    if (after.state.finished) setStatus(db, game.id, 'finished');
    onChange(slug);
    const fresh = getGameBySlug(db, slug)!;
    return reply.send(buildGameView(db, fresh));
  });

  app.post('/api/games/:slug/undo', async (req, reply) => {
    const { slug } = req.params as { slug: string };
    const game = getGameBySlug(db, slug);
    if (!game) return reply.code(404).send({ error: 'nicht gefunden' });
    undoLastThrow(db, game.id);
    // Nach Undo ist das Spiel i.d.R. wieder laufend (Sieg zurückgenommen)
    const after = computeCurrent(db, game.id, game.gameType, game.options);
    setStatus(db, game.id, after.state.finished ? 'finished' : (listThrows(db, game.id).length > 0 ? 'running' : 'lobby'));
    onChange(slug);
    const fresh = getGameBySlug(db, slug)!;
    return reply.send(buildGameView(db, fresh));
  });
}
```

- [ ] **Step 4: `app.ts` erweitern**

`server/src/app.ts` ersetzen durch:
```ts
import Fastify, { type FastifyInstance } from 'fastify';
import type { DB } from './db.js';
import { registerGameRoutes } from './api/games.js';
import { registerPlayRoutes } from './api/play.js';

export interface AppHooks {
  onChange?: (slug: string) => void;
}

export function buildApp(db: DB, now: () => number = () => Date.now(), hooks: AppHooks = {}): FastifyInstance {
  const app = Fastify({ logger: false });
  const onChange = hooks.onChange ?? (() => {});
  registerGameRoutes(app, db, now);
  registerPlayRoutes(app, db, onChange);
  return app;
}
```

- [ ] **Step 5: Tests + Typecheck**

Run: `cd server && npx vitest run && npx tsc --noEmit`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add server/src/api/play.ts server/src/app.ts server/src/__tests__/api-play.test.ts
git commit -m "Backend: Beitreten, Werfen, Undo"
```

---

### Task 6: Verlängern + Cleanup abgelaufener Spiele

**Files:**
- Modify: `server/src/repo.ts`, `server/src/api/play.ts`, `server/src/app.ts`
- Create: `server/src/cleanup.ts`
- Test: `server/src/__tests__/cleanup.test.ts`

**Interfaces:**
- `extendGame(db, gameId, addMs): number` (neues `expiresAt`).
- `deleteExpired(db, now): number` (Anzahl gelöschter Spiele).
- `POST /api/games/:slug/extend` Body `{ duration: '1d'|'1w'|'1M' }` → `200 GameView`.

- [ ] **Step 1: Failing test schreiben**

`server/src/__tests__/cleanup.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { openDb } from '../db.js';
import { createGame, getGameBySlug } from '../repo.js';
import { deleteExpired } from '../cleanup.js';
import { buildApp } from '../app.js';

describe('Verlängern + Cleanup', () => {
  it('deleteExpired entfernt nur abgelaufene Spiele', () => {
    const db = openDb(':memory:');
    const g1 = createGame(db, { gameType: 'x01', options: {}, now: 0 });       // expires 86_400_000
    const g2 = createGame(db, { gameType: 'x01', options: {}, now: 1_000_000_000 });
    const removed = deleteExpired(db, 90_000_000);
    expect(removed).toBe(1);
    expect(getGameBySlug(db, g1.slug)).toBeNull();
    expect(getGameBySlug(db, g2.slug)).not.toBeNull();
  });

  it('extend verschiebt expiresAt um +1 Woche', async () => {
    const app = buildApp(openDb(':memory:'), () => 1000);
    const create = await app.inject({ method: 'POST', url: '/api/games', payload: { gameType: 'x01', options: {}, players: ['A'] } });
    const slug = create.json().slug;
    const res = await app.inject({ method: 'POST', url: `/api/games/${slug}/extend`, payload: { duration: '1w' } });
    expect(res.statusCode).toBe(200);
    expect(res.json().expiresAt).toBe(1000 + 86_400_000 + 7 * 86_400_000);
    await app.close();
  });
});
```

- [ ] **Step 2: Test laufen lassen — muss fehlschlagen**

Run: `cd server && npx vitest run src/__tests__/cleanup.test.ts`
Expected: FAIL — `extendGame`/`deleteExpired`/extend-Route fehlen.

- [ ] **Step 3: Repo + Cleanup + Route implementieren**

Am Ende von `server/src/repo.ts`:
```ts
export function extendGame(db: DB, gameId: number, addMs: number): number {
  const row = db.prepare('SELECT expires_at FROM games WHERE id = ?').get(gameId) as { expires_at: number } | undefined;
  if (!row) throw new Error('game not found');
  const next = row.expires_at + addMs;
  db.prepare('UPDATE games SET expires_at = ? WHERE id = ?').run(next, gameId);
  return next;
}
```

`server/src/cleanup.ts`:
```ts
import type { DB } from './db.js';

export function deleteExpired(db: DB, now: number): number {
  const info = db.prepare('DELETE FROM games WHERE expires_at <= ?').run(now);
  return info.changes;
}

/** Startet einen periodischen Cleanup-Timer. Gibt eine Stop-Funktion zurück. */
export function startCleanup(db: DB, intervalMs = 600_000, now: () => number = () => Date.now()): () => void {
  const timer = setInterval(() => { deleteExpired(db, now()); }, intervalMs);
  timer.unref?.();
  return () => clearInterval(timer);
}
```

In `server/src/api/play.ts` innerhalb `registerPlayRoutes` diese Route ergänzen (Import
oben um `extendGame` erweitern):
```ts
  app.post('/api/games/:slug/extend', async (req, reply) => {
    const { slug } = req.params as { slug: string };
    const game = getGameBySlug(db, slug);
    if (!game) return reply.code(404).send({ error: 'nicht gefunden' });
    const body = req.body as { duration?: '1d' | '1w' | '1M' };
    const map: Record<string, number> = { '1d': 86_400_000, '1w': 7 * 86_400_000, '1M': 30 * 86_400_000 };
    const addMs = map[body?.duration ?? ''];
    if (!addMs) return reply.code(400).send({ error: 'ungültige Dauer' });
    extendGame(db, game.id, addMs);
    onChange(slug);
    return reply.send(buildGameView(db, getGameBySlug(db, slug)!));
  });
```
Import-Zeile in `play.ts` anpassen:
```ts
import {
  getGameBySlug, addPlayer, listPlayers, listThrows, appendThrow, undoLastThrow, setStatus, extendGame,
} from '../repo.js';
```

- [ ] **Step 4: Tests + Typecheck**

Run: `cd server && npx vitest run && npx tsc --noEmit`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add server/src/repo.ts server/src/cleanup.ts server/src/api/play.ts server/src/__tests__/cleanup.test.ts
git commit -m "Backend: Verlängern + Cleanup abgelaufener Spiele"
```

---

### Task 7: WebSocket-Räume + Broadcast

Clients verbinden sich pro Spiel; bei jeder Änderung (`onChange`) wird die aktuelle
`GameView` an alle im Raum gesendet. Beim Verbinden gibt es sofort den aktuellen Stand
(Reconnect-Resync).

**Files:**
- Create: `server/src/realtime.ts`
- Modify: `server/src/app.ts`
- Test: `server/src/__tests__/realtime.test.ts`

**Interfaces:**
- ```ts
  class Rooms {
    join(slug: string, send: (data: string) => void): () => void; // returns leave()
    broadcast(slug: string, data: string): void;
    countForKey(key: string): number; // für Rate-Limit (Task 8)
  }
  ```
- WS-Route `GET /api/games/:slug/ws`: beim Connect Room beitreten + initiale `GameView`
  senden; `onChange` broadcastet an den Raum.

- [ ] **Step 1: Failing test schreiben (Room-Logik als Unit)**

`server/src/__tests__/realtime.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { Rooms } from '../realtime.js';

describe('Rooms', () => {
  it('broadcastet an alle im selben Raum, nicht an andere', () => {
    const rooms = new Rooms();
    const a: string[] = []; const b: string[] = []; const other: string[] = [];
    rooms.join('g1', (d) => a.push(d));
    rooms.join('g1', (d) => b.push(d));
    rooms.join('g2', (d) => other.push(d));
    rooms.broadcast('g1', 'hello');
    expect(a).toEqual(['hello']);
    expect(b).toEqual(['hello']);
    expect(other).toEqual([]);
  });
  it('leave entfernt den Client', () => {
    const rooms = new Rooms();
    const a: string[] = [];
    const leave = rooms.join('g1', (d) => a.push(d));
    leave();
    rooms.broadcast('g1', 'x');
    expect(a).toEqual([]);
  });
});
```

- [ ] **Step 2: Test laufen lassen — muss fehlschlagen**

Run: `cd server && npx vitest run src/__tests__/realtime.test.ts`
Expected: FAIL — `Rooms` fehlt.

- [ ] **Step 3: `realtime.ts` implementieren**

`server/src/realtime.ts`:
```ts
type Sender = (data: string) => void;

export class Rooms {
  private rooms = new Map<string, Set<Sender>>();

  join(slug: string, send: Sender): () => void {
    let set = this.rooms.get(slug);
    if (!set) { set = new Set(); this.rooms.set(slug, set); }
    set.add(send);
    return () => {
      const s = this.rooms.get(slug);
      if (!s) return;
      s.delete(send);
      if (s.size === 0) this.rooms.delete(slug);
    };
  }

  broadcast(slug: string, data: string): void {
    const set = this.rooms.get(slug);
    if (!set) return;
    for (const send of set) send(data);
  }
}
```

- [ ] **Step 4: `app.ts` um WebSocket + Broadcast erweitern**

`server/src/app.ts` ersetzen durch:
```ts
import Fastify, { type FastifyInstance } from 'fastify';
import websocket from '@fastify/websocket';
import type { DB } from './db.js';
import { registerGameRoutes, buildGameView } from './api/games.js';
import { registerPlayRoutes } from './api/play.js';
import { getGameBySlug } from './repo.js';
import { Rooms } from './realtime.js';

export function buildApp(db: DB, now: () => number = () => Date.now()): FastifyInstance {
  const app = Fastify({ logger: false });
  const rooms = new Rooms();

  const onChange = (slug: string): void => {
    const game = getGameBySlug(db, slug);
    if (!game) return;
    rooms.broadcast(slug, JSON.stringify(buildGameView(db, game)));
  };

  app.register(async (scoped) => {
    await scoped.register(websocket);
    scoped.get('/api/games/:slug/ws', { websocket: true }, (socket, req) => {
      const { slug } = req.params as { slug: string };
      const game = getGameBySlug(db, slug);
      if (!game) { socket.close(); return; }
      const leave = rooms.join(slug, (data) => { try { socket.send(data); } catch { /* geschlossen */ } });
      socket.send(JSON.stringify(buildGameView(db, game))); // initialer Resync
      socket.on('close', leave);
    });
  });

  registerGameRoutes(app, db, now);
  registerPlayRoutes(app, db, onChange);
  return app;
}
```

- [ ] **Step 5: Tests + Typecheck**

Run: `cd server && npx vitest run && npx tsc --noEmit`
Expected: PASS (Room-Unit-Tests grün; die bestehenden inject-Tests laufen weiter, da
`onChange` intern gesetzt ist).

- [ ] **Step 6: Commit**

```bash
git add server/src/realtime.ts server/src/app.ts server/src/__tests__/realtime.test.ts
git commit -m "Backend: WebSocket-Räume + Live-Broadcast"
```

---

### Task 8: Rate-Limiting + globaler Deckel + Server-Einstieg

**Files:**
- Modify: `server/src/app.ts`, `server/src/repo.ts`
- Create: `server/src/server.ts`
- Test: `server/src/__tests__/api-games.test.ts` (Ergänzung)

**Interfaces:**
- `countActiveGames(db, now): number`.
- `POST /api/games` gibt `429` bei überschrittenem globalem Deckel; `@fastify/rate-limit`
  begrenzt Erstellungen pro IP.

- [ ] **Step 1: Failing test ergänzen**

In `server/src/__tests__/api-games.test.ts` anfügen:
```ts
import { createGame } from '../repo.js';
import { openDb as openDb2 } from '../db.js';

describe('API: globaler Deckel', () => {
  it('429 wenn zu viele aktive Spiele', async () => {
    const db = openDb2(':memory:');
    // Deckel künstlich niedrig via ENV
    process.env.MAX_ACTIVE_GAMES = '1';
    const { buildApp } = await import('../app.js?cap');
    createGame(db, { gameType: 'x01', options: {}, now: Date.now() });
    const app = buildApp(db);
    const res = await app.inject({ method: 'POST', url: '/api/games', payload: { gameType: 'x01', options: {}, players: ['A'] } });
    expect(res.statusCode).toBe(429);
    await app.close();
    delete process.env.MAX_ACTIVE_GAMES;
  });
});
```
*(Der Query-Suffix `?cap` erzwingt einen frischen Modul-Import, damit die ENV gelesen
wird. Falls das in eurer Vitest-Konfig nicht greift: `vi.resetModules()` vor dem Import.)*

- [ ] **Step 2: Test laufen lassen — muss fehlschlagen**

Run: `cd server && npx vitest run src/__tests__/api-games.test.ts`
Expected: FAIL — Deckel nicht implementiert (kein 429).

- [ ] **Step 3: `countActiveGames` im Repo**

Am Ende von `server/src/repo.ts`:
```ts
export function countActiveGames(db: DB, now: number): number {
  const row = db.prepare('SELECT COUNT(*) AS n FROM games WHERE expires_at > ?').get(now) as { n: number };
  return row.n;
}
```

- [ ] **Step 4: Rate-Limit + Deckel in `app.ts`**

In `server/src/app.ts`: `@fastify/rate-limit` registrieren und im `POST /api/games` den
Deckel prüfen. Konkret in `buildApp` vor den Routen ergänzen:
```ts
import rateLimit from '@fastify/rate-limit';
```
und innerhalb `buildApp` (nach `const rooms = ...`):
```ts
  const maxActive = Number(process.env.MAX_ACTIVE_GAMES ?? 10_000);
  app.register(rateLimit, {
    global: false,
    max: Number(process.env.CREATE_RATE_MAX ?? 20),
    timeWindow: '1 hour',
  });
```
Und die Erstellungs-Route erhält den Deckel-Check. Dazu `registerGameRoutes` um einen
`maxActive`-Parameter erweitern — Signatur in `api/games.ts` anpassen:
```ts
export function registerGameRoutes(app: FastifyInstance, db: DB, now: () => number, maxActive: number): void {
```
und im Handler zu Beginn von `POST /api/games`:
```ts
    const active = countActiveGames(db, now());
    if (active >= maxActive) return reply.code(429).send({ error: 'Serverlimit erreicht' });
```
(Import `countActiveGames` in `api/games.ts` ergänzen.) In `app.ts` den Aufruf anpassen:
```ts
  registerGameRoutes(app, db, now, maxActive);
```
Die Rate-Limit-Config nur auf die Create-Route anwenden — dazu die Route mit
`config: { rateLimit: {} }` markieren:
```ts
  app.post('/api/games', { config: { rateLimit: { max: Number(process.env.CREATE_RATE_MAX ?? 20), timeWindow: '1 hour' } } }, async (req, reply) => {
```

- [ ] **Step 5: `server.ts` (Einstieg)**

`server/src/server.ts`:
```ts
import { openDb } from './db.js';
import { buildApp } from './app.js';
import { startCleanup } from './cleanup.js';

const db = openDb(process.env.DB_PATH ?? './data/dart.sqlite');
const app = buildApp(db);
startCleanup(db);

const port = Number(process.env.PORT ?? 3000);
app.listen({ port, host: '0.0.0.0' })
  .then(() => console.log(`Dart-Counter API auf :${port}`))
  .catch((err) => { console.error(err); process.exit(1); });
```
Und in `server/package.json` `scripts` ergänzen:
```json
    "build": "tsc",
    "start": "node dist/server.js"
```

- [ ] **Step 6: Tests + Typecheck**

Run: `cd server && npx vitest run && npx tsc --noEmit`
Expected: PASS (inkl. 429-Test), kein Typfehler.

- [ ] **Step 7: Commit**

```bash
git add server/
git commit -m "Backend: Rate-Limiting, globaler Deckel, Server-Einstieg"
```

---

## Self-Review

**Spec-Abdeckung (Design-Doc §3, §9, §10, §11, §13a):**
- Ein Container, Node/TS + SQLite ✓; unratbarer Slug ✓ (Task 1)
- Event-Log als Quelle der Wahrheit, Zustand berechnet ✓ (Task 4/5)
- Erstellen/Lesen ✓ (Task 4); Beitreten/Werfen/Undo ✓ (Task 5)
- „Jede*r darf eintragen" → Server bestimmt aktuellen Spieler ✓ (Task 5)
- Mid-Game-Beitritt vermerkt `joinedAtRound` + `catchUp` ✓ (Task 5) — **die
  eigentliche Aufhol-Mechanik (Extra-Darts) ist noch nicht in der Engine** → Plan 5
  (Refinement), siehe unten.
- Verlängern (+1d/+1w/+1M) ✓ (Task 6); 24h-Cleanup ✓ (Task 6)
- WebSocket-Broadcast + Reconnect-Resync ✓ (Task 7)
- Rate-Limiting (IP) + globaler Deckel ✓ (Task 8)

**Placeholder-Scan:** kein TBD/TODO; jeder Code-Schritt vollständig. ✓

**Typ-Konsistenz:** `GameRow`/`PlayerRow`/`EventRow`, `buildGameView`/`GameView`,
`registerGameRoutes`/`registerPlayRoutes`, `Rooms`, `computeGameState` durchgängig
konsistent. ✓

**Bewusst offen → Plan 5 (Refinement):** Aufhol-Mechanik (Engine-seitige Extra-Darts
für mid-game beigetretene Spieler; §10 des Designs). Erfordert variable Zuglänge in
`groupTurns`/den Engines und wird separat geplant, um dieses Backend schlank zu halten.
Die Datenfelder (`joinedAtRound`, `catchUp`) sind bereits vorhanden.
