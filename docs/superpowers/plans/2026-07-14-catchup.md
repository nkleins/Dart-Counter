# Aufhol-Mechanik (Catch-Up) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Spieler*innen, die mit Option „Aufholen" mitten im Spiel beitreten, bekommen
bei ihrem **ersten Zug** so viele Extra-Darts, dass sie danach gleich viele Darts wie
die anderen geworfen haben (an der Runde ausgerichtet). „Handicap"-Beitritt bleibt beim
normalen 3-Dart-Zug.

**Architecture:** Der einzige Hebel ist die **variable Zuglänge des ersten Zugs**. Wir
erweitern `groupTurns` um eine pro-Spieler-Obergrenze für den ersten Zug (`cap` je Turn);
alle Engines nutzen `turn.cap` statt der festen 3 zur Zug-Abschluss-Erkennung. Das
Backend leitet die Erst-Zug-Länge aus `catchUp` + `joinedAtRound` ab.

**Tech Stack:** wie Plan 1–3.

**Voraussetzung:** Plan 1–3 umgesetzt.

## Global Constraints

- Wie Plan 1 (TS strict, ESM, Node 20).
- Erst-Zug-Länge (Darts) für eine mit „Aufholen" in Runde `r` (1-basiert) beigetretene
  Person: `firstTurnDarts = r * 3` (d.h. `3 + (r-1)*3` Extra-Darts für die verpassten
  `r-1` Runden). „Handicap" und von-Beginn-Spieler: `3`.
- Commits: nur Meilensteine, Autor nur User.

---

## File Structure

```
server/src/engine/
  turns.ts            # groupTurns: pro-Spieler cap + Turn.cap
  types.ts            # PlayerInput.firstTurnDarts (optional)
  x01.ts              # turn.cap statt 3
  cricket.ts          # turn.cap statt 3
  atc.ts              # turn.cap statt 3
server/src/
  repo.ts / api       # firstTurnDarts aus catchUp/joinedAtRound ableiten
```

---

### Task 1: `groupTurns` mit pro-Spieler-Erstzug-Cap

**Files:**
- Modify: `server/src/engine/turns.ts`, `server/src/engine/types.ts`
- Test: `server/src/engine/__tests__/turns.test.ts`

**Interfaces:**
- `Turn` erhält Feld `cap: number` (max. Darts dieses Zugs).
- `groupTurns(darts: PlayerDart[], firstTurnDarts?: Map<string, number>): Turn[]` — der
  **erste** Zug eines Spielers darf bis `firstTurnDarts.get(id) ?? 3` Darts fassen, jeder
  weitere Zug 3.
- `PlayerInput` erhält optionales Feld `firstTurnDarts?: number`.

- [ ] **Step 1: Failing tests ergänzen**

In `server/src/engine/__tests__/turns.test.ts` anfügen:
```ts
describe('groupTurns — Erstzug-Cap (Aufholen)', () => {
  it('erster Zug fasst bis firstTurnDarts, danach 3', () => {
    const caps = new Map<string, number>([['a', 6]]);
    // a wirft 8 Darts hintereinander -> Zug1 (6) + Zug2 (2)
    const darts = Array.from({ length: 8 }, () => d('a', 1, 1));
    const turns = groupTurns(darts, caps);
    expect(turns.map((t) => t.darts.length)).toEqual([6, 2]);
    expect(turns[0]!.cap).toBe(6);
    expect(turns[1]!.cap).toBe(3);
  });
  it('ohne Cap bleibt es bei 3', () => {
    const darts = Array.from({ length: 4 }, () => d('a', 1, 1));
    const turns = groupTurns(darts);
    expect(turns.map((t) => t.darts.length)).toEqual([3, 1]);
    expect(turns[0]!.cap).toBe(3);
  });
});
```

- [ ] **Step 2: Test laufen lassen — muss fehlschlagen**

Run: `cd server && npx vitest run src/engine/__tests__/turns.test.ts`
Expected: FAIL — `cap` fehlt / Cap-Parameter nicht unterstützt.

- [ ] **Step 3: `types.ts` erweitern**

In `server/src/engine/types.ts` das `PlayerInput`-Interface ersetzen:
```ts
export interface PlayerInput {
  id: string;
  name: string;
  order: number;
  firstTurnDarts?: number; // Erst-Zug-Länge (Default 3); >3 = Aufholen
}
```

- [ ] **Step 4: `groupTurns` + `Turn` anpassen**

In `server/src/engine/turns.ts` `Turn` und `groupTurns` ersetzen:
```ts
export interface Turn {
  playerId: string;
  darts: PlayerDart[];
  cap: number;
}

export function groupTurns(darts: PlayerDart[], firstTurnDarts: Map<string, number> = new Map()): Turn[] {
  const turns: Turn[] = [];
  const started = new Set<string>();
  for (const dart of darts) {
    const last = turns[turns.length - 1];
    if (last && last.playerId === dart.playerId && last.darts.length < last.cap) {
      last.darts.push(dart);
    } else {
      const isFirst = !started.has(dart.playerId);
      started.add(dart.playerId);
      const cap = isFirst ? (firstTurnDarts.get(dart.playerId) ?? 3) : 3;
      turns.push({ playerId: dart.playerId, darts: [dart], cap });
    }
  }
  return turns;
}
```

- [ ] **Step 5: Tests + Typecheck**

Run: `cd server && npx vitest run src/engine/__tests__/turns.test.ts && npx tsc --noEmit`
Expected: turns-Tests PASS. (x01/cricket/atc können jetzt Typfehler zeigen, weil sie
`turn.darts.length >= 3` noch nutzen und `groupTurns` ohne Cap-Map aufrufen — beides ist
weiterhin gültig; der Cap greift erst in Task 2. Typecheck bleibt grün, da `cap` additiv.)

- [ ] **Step 6: Commit**

```bash
git add server/src/engine/turns.ts server/src/engine/types.ts server/src/engine/__tests__/turns.test.ts
git commit -m "Engine: groupTurns mit pro-Spieler-Erstzug-Cap"
```

---

### Task 2: Engines nutzen `turn.cap` + `firstTurnDarts`

**Files:**
- Modify: `server/src/engine/x01.ts`, `server/src/engine/cricket.ts`, `server/src/engine/atc.ts`
- Test: `server/src/engine/__tests__/x01.test.ts`

**Interfaces:** `computeX01State`/`computeCricketState`/`computeAtcState`-Signaturen
unverändert; sie lesen `firstTurnDarts` aus den `players` und geben es an `groupTurns`.

- [ ] **Step 1: Failing test ergänzen (x01 stellvertretend)**

In `server/src/engine/__tests__/x01.test.ts` anfügen:
```ts
describe('computeX01State — Aufholen (firstTurnDarts)', () => {
  it('Aufhol-Spieler wirft 6 Darts in einem Zug', () => {
    const players: PlayerInput[] = [
      { id: 'a', name: 'A', order: 0, firstTurnDarts: 6 },
      { id: 'b', name: 'B', order: 1 },
    ];
    // A wirft 6x S20 (=120) am Stück -> ein Zug, danach B
    const darts: PlayerDart[] = Array.from({ length: 6 }, () => d('a', 20, 1));
    const s = computeX01State(OPTS, players, darts);
    expect(s.players.find((p) => p.playerId === 'a')!.remaining).toBe(501 - 120);
    expect(s.currentPlayerId).toBe('b');
    expect(s.dartsThrownThisTurn).toBe(0);
  });
});
```

- [ ] **Step 2: Test laufen lassen — muss fehlschlagen**

Run: `cd server && npx vitest run src/engine/__tests__/x01.test.ts`
Expected: FAIL — ohne Cap-Weitergabe wird A's Zug bei 3 beendet, B fälschlich schon nach 3.

- [ ] **Step 3: `x01.ts` anpassen**

In `computeX01State` den `groupTurns(darts)`-Aufruf ersetzen durch:
```ts
  const firstTurnDarts = new Map(players.filter((p) => p.firstTurnDarts).map((p) => [p.id, p.firstTurnDarts!]));
  const turns = groupTurns(darts, firstTurnDarts);
```
Und in der Replay-Schleife die Zug-Abschluss-Bedingung `turn.darts.length >= 3` ersetzen
durch `turn.darts.length >= turn.cap` (die Zeile mit `lastTurnComplete = busted || wonThisTurn || turn.darts.length >= 3`):
```ts
    if (i === turns.length - 1) lastTurnComplete = busted || wonThisTurn || turn.darts.length >= turn.cap;
```

- [ ] **Step 4: `cricket.ts` anpassen**

Analog in `computeCricketState`:
- `groupTurns(darts)` → mit Cap-Map:
```ts
  const firstTurnDarts = new Map(players.filter((p) => p.firstTurnDarts).map((p) => [p.id, p.firstTurnDarts!]));
  const turns = groupTurns(darts, firstTurnDarts);
```
- Abschluss-Zeile `lastTurnComplete = winnerId === turn.playerId || turn.darts.length >= 3` →
```ts
    if (i === turns.length - 1) lastTurnComplete = winnerId === turn.playerId || turn.darts.length >= turn.cap;
```

- [ ] **Step 5: `atc.ts` anpassen**

Analog in `computeAtcState`:
- `groupTurns(darts)` → mit Cap-Map (wie oben).
- Abschluss-Zeile `lastTurnComplete = winnerId === turn.playerId || turn.darts.length >= 3` →
```ts
    if (i === turns.length - 1) lastTurnComplete = winnerId === turn.playerId || turn.darts.length >= turn.cap;
```

- [ ] **Step 6: Alle Engine-Tests + Typecheck**

Run: `cd server && npx vitest run && npx tsc --noEmit`
Expected: PASS (alle Engine-Tests inkl. neuem Aufhol-Test), kein Typfehler.

- [ ] **Step 7: Commit**

```bash
git add server/src/engine/
git commit -m "Engine: Erstzug-Cap in x01/cricket/atc (Aufhol-Mechanik)"
```

---

### Task 3: Backend leitet `firstTurnDarts` ab

Die Engine-Eingabe wird im Backend gebaut (`buildGameView`, `computeCurrent`). Dort setzen
wir `firstTurnDarts` für Aufhol-Spieler.

**Files:**
- Modify: `server/src/api/games.ts`, `server/src/api/play.ts`
- Test: `server/src/__tests__/api-play.test.ts`

**Interfaces:**
- Neue Hilfe `firstTurnDartsFor(p: PlayerRow): number | undefined` (in `api/games.ts`,
  exportiert): `catchUp === 'catchUp' && joinedAtRound > 0 ? joinedAtRound * 3 : undefined`.

- [ ] **Step 1: Failing test ergänzen**

In `server/src/__tests__/api-play.test.ts` anfügen:
```ts
it('Aufhol-Beitritt in Runde 2 gibt der Person 6 Darts im ersten Zug', async () => {
  const app = buildApp(openDb(':memory:'));
  // 2 Spieler, eine volle Runde spielen (je 3 Darts) -> Runde 2
  const create = await app.inject({
    method: 'POST', url: '/api/games',
    payload: { gameType: 'x01', options: { start: 501, in: 'straight', out: 'straight' }, players: ['Mia', 'Ben'] },
  });
  const slug = create.json().slug;
  const throwN = (seg: number) => app.inject({ method: 'POST', url: `/api/games/${slug}/throws`, payload: { segment: seg, multiplier: 1 } });
  for (let i = 0; i < 6; i++) await throwN(1); // Mia 3 + Ben 3 -> Runde 2, Mia dran
  // Lena tritt auf (Runde 2) mit Aufholen
  const join = await app.inject({ method: 'POST', url: `/api/games/${slug}/players`, payload: { name: 'Lena', catchUp: 'catchUp' } });
  expect(join.statusCode).toBe(201);
  expect(join.json().player.joinedAtRound).toBe(2);

  // Reihenfolge: Mia, Ben, Lena. Erst Mia (3) + Ben (3) fertig, dann Lena.
  for (let i = 0; i < 6; i++) await throwN(20); // Mia 3 + Ben 3
  // Jetzt ist Lena dran und darf 6 Darts werfen (2*3): 5 Darts -> immer noch Lena
  let view;
  for (let i = 0; i < 5; i++) view = (await throwN(5)).json();
  const lena = view.players.find((p: { name: string }) => p.name === 'Lena');
  expect(view.state.currentPlayerId).toBe(lena.id); // nach 5/6 Darts noch dran
  await app.close();
});
```

- [ ] **Step 2: Test laufen lassen — muss fehlschlagen**

Run: `cd server && npx vitest run src/__tests__/api-play.test.ts`
Expected: FAIL — Lena wird schon nach 3 Darts abgelöst (kein Aufhol-Cap).

- [ ] **Step 3: `firstTurnDartsFor` + Einbindung**

In `server/src/api/games.ts`:
1. Hilfsfunktion (exportiert) ergänzen:
```ts
import type { PlayerRow } from '../repo.js';

export function firstTurnDartsFor(p: PlayerRow): number | undefined {
  return p.catchUp === 'catchUp' && p.joinedAtRound > 0 ? p.joinedAtRound * 3 : undefined;
}
```
2. In `buildGameView` die `engineIn`-Erzeugung ersetzen durch:
```ts
  const engineIn: PlayerInput[] = players.map((p) => ({ id: p.id, name: p.name, order: p.order, firstTurnDarts: firstTurnDartsFor(p) }));
```

In `server/src/api/play.ts` in `computeCurrent` die `engineIn`-Erzeugung ebenso ersetzen
(Import `firstTurnDartsFor` aus `./games.js` ergänzen):
```ts
import { buildGameView, firstTurnDartsFor } from './games.js';
// ...
  const engineIn: PlayerInput[] = players.map((p) => ({ id: p.id, name: p.name, order: p.order, firstTurnDarts: firstTurnDartsFor(p) }));
```

- [ ] **Step 4: Tests + Typecheck**

Run: `cd server && npx vitest run && npx tsc --noEmit`
Expected: PASS (inkl. Aufhol-API-Test), kein Typfehler.

- [ ] **Step 5: Commit**

```bash
git add server/src/api/
git commit -m "Backend: Aufhol-Darts aus catchUp/joinedAtRound ableiten"
```

---

## Self-Review

**Spec-Abdeckung (Design-Doc §10):**
- „Aufholen": Extra-Darts im ersten Zug, an Runde ausgerichtet ✓ (Task 1–3)
- „Handicap": normaler 3-Dart-Zug (kein Cap) ✓ (Default)
- Gilt für x01, Cricket, ATC (gemeinsames `turns.ts`) ✓ (Task 2)

**Placeholder-Scan:** kein TBD/TODO; jeder Code-Schritt vollständig. ✓

**Typ-Konsistenz:** `Turn.cap`, `PlayerInput.firstTurnDarts`, `firstTurnDartsFor`
konsistent; `groupTurns`-Signatur additiv (Default-Map), alle bestehenden Aufrufe gültig. ✓

**Hinweis (Frontend):** Der „Dart x / 3"-Zähler in der TurnBar zeigt bei Aufhol-Zügen
weiterhin „/3". Optionaler Feinschliff: Zähler an `turn.cap` koppeln (der Wert steckt
nicht in `GameView.state`; ggf. `dartsThisTurnCap` ergänzen). Nicht blockierend.
